import 'package:flutter/material.dart';

class CustomMoneyDetail extends StatelessWidget {
  const CustomMoneyDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
